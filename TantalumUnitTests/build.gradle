apply plugin: 'java'

sourceSets {
    test {
        java {
            srcDir 'src'
        }
    }
}

dependencies {
    compile project(':TantalumLibrary')
    compile files('../Tantalum4Library/dist/Release/lib/jmunit4cldc11-1.2.1.jar')
}

task compileTestJava(overwrite: true, dependsOn: wtkpackage) << {
}

